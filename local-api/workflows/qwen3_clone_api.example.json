{
  "notice": "Reference only. Use actual ComfyUI API workflow for production.",
  "required_nodes": [
    "Qwen3Loader",
    "LoadAudio",
    "Qwen3VoiceClone",
    "SaveAudio"
  ],
  "runtime_patched_fields": [
    "Qwen3VoiceClone.inputs.text",
    "Qwen3VoiceClone.inputs.ref_text",
    "LoadAudio.inputs.audio",
    "SaveAudio.inputs.filename_prefix"
  ]
}
