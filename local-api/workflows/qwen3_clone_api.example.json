{
  "notice": "This is a non-runnable reference placeholder. Export your real ComfyUI API workflow as qwen3_clone_api.json for production.",
  "required": [
    "Qwen3VoiceClone.inputs.text",
    "SaveAudio node or {{OUTPUT_BASENAME}} token"
  ]
}
