# 運用メモ

## 日常運用

1. API起動 (`start-local-api.ps1`)
2. ChatGPTを開く
3. 右下パネルが `起動中` であることを確認
4. 自動読み上げまたは `最新を読む` を使う

## エンジン切替

- まず `windows_sapi` または `mock_wav` でE2E確認
- 運用時に `comfyui_qwen3` へ切替

## 生成物

- 出力先: `local-api/runtime/audio/`
- Git管理外
