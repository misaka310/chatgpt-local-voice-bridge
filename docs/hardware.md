# 動作環境

Windows と NVIDIA GPU の利用を前提にしています。

## 推奨環境

- Windows
- Python 3.10 以上
- NVIDIA GPU
- VRAM 12GB 以上推奨
- CUDA が使える NVIDIA ドライバー

`setup-voice-env.cmd` は、ブラウザで使う前に CUDA を確認します。

## CPUで試す場合

CPUでの実行は動作確認用です。かなり遅くなるため、公開初回導線では推奨しません。
